number = int(input(""))  #輸入數字，代表隊伍的總數
a = 0  #初始化一個變數a來統計符合規則的隊伍數量，初始值為0

for i in range(number):      #進行number次迴圈，每次迴圈中輸入一個數字 member，代表該隊伍的人數。
    member = int(input("")) 
    if member % 3 == 0 and member > 0:  #查 member是否是3的倍數且大於 0，如果是則將a加一。     
        a = a + 1   
print(a)  #迴圈結束後，輸出a，即符合標準的隊伍總數。
